import ChatExperience from "./chat-experience";

/**
 * ChatPage는 사용자가 구매 조건을 입력하고 추천 후보를 확인하는 대화 화면을 제공한다.
 * 현재는 화면 검증용 예시 데이터를 사용하며 이후 Agent Gateway 응답으로 교체한다.
 */
export default function ChatPage() {
  return <ChatExperience />;
}
