import CollectionsDashboard from "./collections-dashboard";

/** CollectionsAdminPage는 수집 job/작업, 판매처 상품, 검증 현황을 최근 24시간 기준으로 보여준다. */
export default function CollectionsAdminPage() {
  return <CollectionsDashboard />;
}
