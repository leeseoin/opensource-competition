# 업무보고 - 2026-08-04

## 업무내용
1) Python 크롤러 비교 환경 구성
2) JSON/HTML 수집 결과 검증
3) Spring Boot DB 적재 연동

## 진행상황

### 1.1) 정우님 Python 크롤러 가져오기
- `dev-jw` 코드를 비교 실험용 `sandbox-python-crawler/ls` 브랜치로 가져왔으며 원본 브랜치는 변경하지 않음
- ABC마트/29CM 검색 및 Spring Boot 수동 적재 API 연결 완료

### 2.1) 수집 결과 교차 검증
- ABC마트는 검색 JSON과 렌더링 HTML, 29CM는 검색 JSON과 상품 상세 JSON-LD를 요청 상품 전체에 대해 비교하도록 구성
- 검증 결과를 `MATCHED`/`MISMATCH`/`PENDING`으로 구분하고 원본 JSON/HTML은 판매처별 디렉터리에 저장

### 3.1) 검증 결과 DB 적재
- Flyway V3로 `product_verifications` 테이블을 추가하고 상품/스냅샷/옵션/근거/검증 결과 저장을 연결
- ABC마트 500개 DB 적재와 ABC마트/29CM 3개 검증 테스트를 수동으로 확인

## To do list
1) Python과 동일한 입력/출력/검증 기준을 `sandbox/ls` Go Collector에 구현
2) 29CM 옵션 수집 미구현 범위와 공통 Contract 필드를 정우님과 합의
3) 동일 상품 수/페이지/요청 간격으로 Python과 Go 성능을 비교
