# 2026-08-15 VERIFY-001 구매 직전 재검증 E2E

- 기능 ID: `VERIFY-001`
- 구현 commit: `d1a70a0`
- 기록 상태: 구현 기록

## 배경과 범위

기존 재검증 구현의 READY부터 COMPLETED 상태 전이를 자동 검증하고 실제 Web/MCP/Backend/
Python Worker 경로에서 가격과 재고 재검증을 실행했다.

## 구현 내용

- `services/product-backend/src/test/java/com/purchasesearch/product_backend/agentrun/service/AgentRunServiceTests.java:187` `verifiesSelectedReadyCandidateAndCompletesRun`: READY 후보 선택/VERIFYING/VERIFIED/COMPLETED 상태 전이 검증

## 발생 문제와 원인

기존 라이브 평가의 재검증 대상 질문이 모두 NO_RESULTS여서 구매 직전 재검증을 실행하지 못했다.

## 해결

READY가 된 운동화 후보를 선택해 Web API가 MCP 도구를 거쳐 우선 수집을 요청하도록 하고,
Python Worker 결과를 Backend가 저장한 뒤 Agent Run을 `COMPLETED`로 전환하는 것을 확인했다.

## 검증

- `cd services/product-backend && ./gradlew test`: 전체 통과
- 실제 실행 `29259fc6-17f8-42c3-bee1-8f69010ca055`: `VERIFICATION_REQUESTED`/`VERIFICATION_COMPLETED`, 최종 `VERIFIED`, 변경 항목 0개

## 남은 작업

상품 상세 수집 계약으로 옵션 단위 가격과 재고를 직접 재검증하는 기능이 남아 있다.
